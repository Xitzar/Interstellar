export class MonteCarlo {
    /** From given state, repeatedly run MCTS to build statistics. */
    runSearch(state : any, timeout : any) {
      // TODO
    }
    /** Get the best move from available statistics. */
    bestPlay(state : any) {
      // TODO
      // return play
    }
  }