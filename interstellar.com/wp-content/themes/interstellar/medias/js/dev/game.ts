
/** Class representing the game board. */
export class Game {

    /** Generate and return the initial game state. */
    start() {
      // TODO
      let state = false
      return state
    }
    /** Return the current player’s legal moves from given state. */
    legalPlays(state : any) {
      // TODO
      let plays = false
      return plays
    }
    /** Advance the given state and return it. */
    nextState(state : any, move : any) {
      // TODO
      let newState = false
      let newState2 = state
      let newState3 = move
      return newState
    }
    /** Return the winner of the game. */
    winner(state : any) {
      // TODO
      let winner = false
      return winner
    }
  }
  
  //module.exports = Game